# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS generated files
.DS_Store
Thumbs.db

# Temporary files
*.tmp
*.temp

# IDE and editor files
.idea/
.vscode/
*.swp
*.swo

# Environment variable files (not typical for HTML/CSS/JS but included for safety)
.env
.env.local
.env.*.local

# Build tools (if added in the future)
node_modules/
dist/
build/

# Dependencies (not typical, but added for safety)
package-lock.json
yarn.lock

# Sass/CSS cache files (if you use Sass in the future)
.sass-cache/
*.css.map
*.scss.map

# Output files
*.out
*.log

# GitHub Pages cache
/.cache/

# Temporary server files (if used for preview)
*.tmp/